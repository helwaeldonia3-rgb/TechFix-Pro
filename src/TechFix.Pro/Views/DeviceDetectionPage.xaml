<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TechFix.Pro.Views.DeviceDetectionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="20" RowSpacing="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="Device Detection" FontSize="28" FontWeight="Bold" />

        <!-- Scan Button -->
        <Button Grid.Row="1" Content="Scan Devices" Padding="10" />

        <!-- Devices List -->
        <ScrollView Grid.Row="2">
            <StackPanel Spacing="10">
                <TextBlock Text="Connected Devices" FontSize="18" FontWeight="Bold" />
                <ListView SelectionMode="Single" MaxHeight="500">
                    <ListViewItem>
                        <StackPanel Orientation="Vertical" Spacing="5">
                            <TextBlock Text="Device Name" FontWeight="Bold" />
                            <TextBlock Text="Manufacturer: Samsung" FontSize="12" />
                            <TextBlock Text="Model: Galaxy S21" FontSize="12" />
                            <TextBlock Text="SoC: Snapdragon 888" FontSize="12" />
                            <TextBlock Text="Status: Connected" FontSize="12" Foreground="Green" />
                        </StackPanel>
                    </ListViewItem>
                </ListView>
            </StackPanel>
        </ScrollView>
    </Grid>
</Page>