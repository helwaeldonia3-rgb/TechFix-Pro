<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TechFix.Pro.Views.Platforms.SamsungPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="20" RowSpacing="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Spacing="10">
            <TextBlock Text="Samsung" FontSize="28" FontWeight="Bold" />
            <TextBlock Text="Manage Samsung devices (Exynos & Qualcomm)" Foreground="{ThemeResource TextFillColorSecondary}" />
        </StackPanel>

        <!-- Quick Actions -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="10">
            <Button Content="Detect Device" Padding="12,8" />
            <Button Content="Download Mode" Padding="12,8" />
            <Button Content="Flash via Odin" Padding="12,8" />
            <Button Content="Install ADB" Padding="12,8" />
        </StackPanel>

        <!-- Content -->
        <ScrollView Grid.Row="2">
            <StackPanel Spacing="20">
                <!-- Device Info -->
                <Border Background="{ThemeResource CardBackgroundFillColorDefault}" CornerRadius="8" Padding="16">
                    <StackPanel Spacing="12">
                        <TextBlock Text="Device Information" FontSize="16" FontWeight="Bold" />
                        <Grid ColumnSpacing="20" RowSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Series" FontSize="12" Foreground="{ThemeResource TextFillColorSecondary}" />
                                <TextBlock Text="Status: Not Connected" FontSize="14" />
                            </StackPanel>
                            <StackPanel>
                                <TextBlock Text="Region Code" FontSize="12" Foreground="{ThemeResource TextFillColorSecondary}" />
                                <TextBlock Text="--" FontSize="14" />
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Samsung Specific Tools -->
                <Border Background="{ThemeResource CardBackgroundFillColorDefault}" CornerRadius="8" Padding="16">
                    <StackPanel Spacing="12">
                        <TextBlock Text="Available Tools" FontSize="16" FontWeight="Bold" />
                        <ListView SelectionMode="None">
                            <ListViewItem>
                                <StackPanel Orientation="Horizontal" Spacing="10">
                                    <TextBlock Text="✓" Foreground="Green" />
                                    <TextBlock Text="Samsung USB Driver" />
                                </StackPanel>
                            </ListViewItem>
                            <ListViewItem>
                                <StackPanel Orientation="Horizontal" Spacing="10">
                                    <TextBlock Text="✓" Foreground="Green" />
                                    <TextBlock Text="Odin3" />
                                </StackPanel>
                            </ListViewItem>
                            <ListViewItem>
                                <StackPanel Orientation="Horizontal" Spacing="10">
                                    <TextBlock Text="✓" Foreground="Green" />
                                    <TextBlock Text="ADB (Android Debug Bridge)" />
                                </StackPanel>
                            </ListViewItem>
                        </ListView>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollView>
    </Grid>
</Page>